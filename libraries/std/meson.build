subdir('src')

libstd_includes = include_directories('include')

libstd = library('std',
    c_pch: nitro_pch,
    include_directories: [libtwl_includes, libstd_includes],
    pic: build_target == 'nx',
    sources: [libstd_srcs, fx_const_h],
    native: (build_target == 'linux' or build_target == 'win64')
)

libtwl_libs += libstd
