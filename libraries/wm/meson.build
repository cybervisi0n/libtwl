subdir('src')

libwm_includes = include_directories('include')

libwm = library('wm',
    c_pch: nitro_pch,
    include_directories: [libtwl_includes, libwm_includes],
    pic: build_target == 'nx',
    sources: [libwm_srcs, fx_const_h],
    native: (build_target == 'linux' or build_target == 'win64')
)

libtwl_libs += libwm
